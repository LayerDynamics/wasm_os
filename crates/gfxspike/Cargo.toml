[package]
name = "gfxspike"
edition.workspace = true
version.workspace = true
license.workspace = true

# Desktop compositor surface fixture (FR-23): requests a canvas surface via the
# Gpu capability, draws a recognizable pattern, and presents it. Exercised by
# e2e/surface.spec.ts (and, with input, e2e/input.spec.ts in brokered input).
[[bin]]
name = "gfxspike"
path = "src/main.rs"

[dependencies]
wasmos-sys = { path = "../wasmos-sys" }
