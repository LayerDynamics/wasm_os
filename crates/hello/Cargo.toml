[package]
name = "hello"
edition.workspace = true
version.workspace = true
license.workspace = true

# A stock wasm32-wasip1 binary — imports only standard wasi_snapshot_preview1.
# Runs as a process under the WASI process runtime kernel (FR-9).
[[bin]]
name = "hello"
path = "src/main.rs"
